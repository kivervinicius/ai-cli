import React, { useState } from 'react';
import { FolderGit2, Plus, Sparkles } from 'lucide-react';
import { Button, Card, Input } from '../../design-system';
import { nexus } from '../../nexus/api';
import type { Project } from '../../types';
import { useTranslation } from 'react-i18next';
export const ProjectHub: React.FC<{ onCreated: (project: Project) => void }> = ({ onCreated }) => { const { t }=useTranslation(); const [path,setPath]=useState(''); const [busy,setBusy]=useState(false); const [error,setError]=useState(''); const create=async()=>{if(!path.trim())return;setBusy(true);setError('');try{onCreated(await nexus.createProject(path.trim()));setPath('');}catch(e){setError(e instanceof Error?e.message:String(e));}finally{setBusy(false);}}; return <div className="nx-project-hub"><div className="nx-project-hub__brand"><span className="nx-brand-mark nx-brand-mark--hero">N</span><span className="nx-eyebrow">IAPro Nexus · Powered by Maestro</span><h1>{t('projects.headline')}</h1><p>{t('projects.intro')}</p></div><Card className="nx-project-hub__create"><div className="nx-work-card-icon"><FolderGit2 size={20}/></div><div><strong>{t('projects.addRepo')}</strong><small>{t('projects.addDescription')}</small></div><Input value={path} onChange={setPath} onEnter={create} placeholder="/path/to/repository" mono/>{error&&<p className="nx-error-copy">{error}</p>}<Button tone="brand" disabled={!path.trim()||busy} onClick={create}><Plus size={14}/>{busy?t('projects.adding'):t('projects.add')}</Button></Card><div className="nx-project-hub__tip"><Sparkles size={15}/><span>{t('projects.tip')}</span></div></div>; };
