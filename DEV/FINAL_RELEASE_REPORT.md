# AI CLI — Final Release Report

**Date:** 2026-08-28 · **Branch:** `fix/control-production-readiness`

## Version source (single truth)

- `internal/buildinfo` — `Version`, `Commit`, `BuildDate` injected via ldflags;
  `String()` and `JSON()` for `ai version`.
- Removed hardcoded `const version` in `internal/app` (ldflags `-X` cannot override a
  const — this was a real bug).
- `VERSION` file → `0.4.0` (aligned with tag `v0.4.0` and package.json).
- All consumers: `ai version`, `ai version --json`, usage banner, `ai export`,
  `ai report` now read `buildinfo`.

## Artifact matrix (GoReleaser snapshot, verified)

| Target | Archive | Present |
|---|---|---|
| linux/amd64 | `ai-cli_Linux_x86_64.tar.gz` | ✅ |
| linux/arm64 | `ai-cli_Linux_arm64.tar.gz` | ✅ |
| darwin/amd64 | `ai-cli_Darwin_x86_64.tar.gz` | ✅ |
| darwin/arm64 | `ai-cli_Darwin_arm64.tar.gz` | ✅ |
| windows/amd64 | `ai-cli_Windows_x86_64.zip` | ✅ |
| windows/arm64 | `ai-cli_Windows_arm64.zip` | ✅ |
| checksums | `dist/checksums.txt` (6 entries) | ✅ |

Verified extracted linux binary:
```json
{
  "version": "0.4.1-next",
  "commit": "7ac2776837dc32b809e2d98c3e42fc857d07024a",
  "build_date": "2026-08-28T20:49:28Z",
  "go": "go1.25.0",
  "os": "linux",
  "arch": "amd64"
}
```

## Go version contract (aligned)

- `go.mod`: `go 1.25.0`
- CI: Go `1.25`
- README badges: `Go 1.25+`
- `install.sh`: `>=1.25`
- `install.ps1`: no version pin (uses whatever Go is installed)

## Installers

- `install.sh`: Linux/Darwin, `x86_64`/`arm64`, release-download → source fallback.
  Archive naming matches GoReleaser output.
- `install.ps1`: Windows/Darwin/Linux, `x86_64`/`arm64`, zip/tar.gz handling,
  PATH registration, PowerShell completion hint.
- `uninstall.sh` present.

## CI release gate

`.github/workflows/ci.yml` adds a `snapshot` job (needs all test jobs + frontend):
GoReleaser `--snapshot --clean` + artifact/checksum assertions for all 6 targets.

## Frontend tooling (canonical)

- Package manager: **Bun** (locked `bun.lock`, CI `--frozen-lockfile`).
- TypeScript pinned `5.9.3`; ESLint (flat config) + Vitest added; `lint`/`test`
  scripts; CI runs typecheck → lint → test → build.
- Frontend embedded at build time; no Bun/Node required at runtime (single `ai`
  binary — verified `ai version` on a self-contained release binary).

## Honest notes

- Snapshot version `0.4.1-next` is generated by GoReleaser; tagged releases will carry
  the real tag version. ldflags injection verified end-to-end.
