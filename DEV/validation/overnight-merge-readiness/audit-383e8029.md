# Auditoria de merge readiness — `feat/nexus-maximum-delivery`

Branch auditado: `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager` @ `3760df2`  
Base vs `main`: ~189 commits, arquivos centrais (`mission_executor.go`, `plan_application.go`, `runtime_routing.go`) **não existem em `main`** — achados abaixo são predominantemente **INTRODUCED_BY_BRANCH**.

Testes focados executados: `go test ./internal/nexus/... -run 'PinFailsClosed|AllocatePin|ValidationEvidence|RoutingDecision'` — **PASS**.

---

## Resumo executivo

| Área | Veredito merge |
|------|----------------|
| PIN fail-closed em `Allocate` | Implementado no caminho de produção; cobertura só unitária |
| Intent routing silencioso | **Gap HIGH** — falhas engolidas |
| Evidence FAIL vs NOT_VERIFIED | Semântica intencional, com edge case em non-git |
| HTTP routing 404 vs 500 | Coerente, mas intent omitido silenciosamente |
| Quota RATE_LIMITED | Preservação correta e testada |
| Maestro lifecycle no runner | **Não wired** — só transporte de skills |
| Contratos web ↔ nexus | Lacunas em intent e tipos TS |

**Recomendação:** **NOT_READY_FOR_MAIN** até corrigir **MR-002** (intent silencioso). Demais itens são HIGH/MEDIUM com mitigação parcial.

---

## Achados

### MR-001 — PIN fail-closed: produção OK, prova incompleta

| Campo | Valor |
|-------|-------|
| **Severity** | MEDIUM |
| **File:line** | `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/internal/nexus/mission_executor.go:102-138`, `:401-416` |
| **Evidence** | `Allocate()` chama `selectMissionCandidateAccounts(..., isFailover)`; em PIN + failover retorna `ErrBlockedResource` antes de abrir pool; na 1ª tentativa PIN indisponível, `RecommendResources` falha → `ErrBlockedResource` (L135-136). `isFailover` aciona em `Attempt>1 && (RetryFrom==StateAllocating \|\| isQuotaOrRateLimitFailure(...))`. Teste `TestMissionAllocatePinFailsClosedAfterRateLimit` cobre só o helper, não `nexusPackageExecutor.Allocate`. |
| **Impact** | PIN não abre pool alternativo em produção no caminho analisado; risco residual é regressão não detectada por falta de teste E2E de `Allocate`. `ResolveRuntimeRouting` (`runtime_routing.go`) é caminho paralelo **não usado** por `Allocate` — drift arquitetural. |
| **Origin** | INTRODUCED_BY_BRANCH |
| **Recommended fix** | Adicionar teste de integração `Allocate` com `PolicyManual` + conta rate-limited + retry; considerar unificar seleção via `ResolveRuntimeRouting` ou documentar dual-path. |

---

### MR-002 — Intent routing: falhas silenciosas em `ensureIntentDecisionFacts`

| Campo | Valor |
|-------|-------|
| **Severity** | **HIGH** |
| **File:line** | `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/internal/nexus/plan_application.go:85-92` |
| **Evidence** | `DecideIntentForProject` e `PersistIntentDecisionFacts` com erro → `return out` sem log, métrica ou erro ao caller. Todo `PlanApplicationService.Create` (Web POST `/plans`, CLI) passa por aqui. Contraste: `app.go:1607-1609` propaga erro de persistência de intent. |
| **Impact** | Planos criados **sem** `nexus.intent_decision` em `structured_facts`; `/api/v1/runs/{id}/routing` perde bloco `intent`; roteamento fica inexplicável sem sinal ao cliente. Falha silenciosa viola contrato de “intent decision facts on every WorkPlan creation path”. |
| **Origin** | INTRODUCED_BY_BRANCH |
| **Recommended fix** | Fail-closed: retornar erro de `Create` se intent não puder ser persistido; ou persistir outcome explícito (`intent_decision_status=FAILED`) e expor no routing report. Remover swallow silencioso. |

---

### MR-003 — Evidence: FAIL mascarado como NOT_VERIFIED em non-git

| Campo | Valor |
|-------|-------|
| **Severity** | MEDIUM |
| **File:line** | `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/internal/nexus/validation_evidence.go:126-132` |
| **Evidence** | Switch prioriza `HeadSHA==""` → `NOT_VERIFIED` **antes** de `!passed` → `FAIL`. Teste confirma: pass em non-git → `NOT_VERIFIED` (`validation_evidence_test.go:42-43`); fail **com git** → `FAIL` (`:107-108`). Não há teste fail+non-git. |
| **Impact** | Verificação que falha em workspace sem git (ou sem HEAD) grava `NOT_VERIFIED`/`OBSERVED` em vez de `FAIL` — auditoria e gates podem tratar falha real como “não verificado”. |
| **Origin** | INTRODUCED_BY_BRANCH |
| **Recommended fix** | Reordenar: se `!passed` → `FAIL` independente de `HeadSHA`; reservar `NOT_VERIFIED` para `len(results)==0` ou identidade indeterminável **sem** falha assertiva. |

---

### MR-004 — HTTP routing: 404/500 corretos, degradação silenciosa de intent

| Campo | Valor |
|-------|-------|
| **Severity** | MEDIUM |
| **File:line** | `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/internal/control/web/handlers_planning.go:658-670`; `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/internal/nexus/run_application.go:92-95` |
| **Evidence** | Run inexistente → 404; JSON corrupto em routing/intent → 500 via `BuildRoutingDecisionReportWithPlan`; stream ausente → 200 + `chain_verified=false` (correto). Porém `GetWorkPlan` falha → **silenciosamente** omite `intent` e retorna 200 com só `decisions`. Sem testes HTTP para `/routing` ou `/validation-evidence` (só `web/src/nexus/api.test.ts` mock). |
| **Impact** | Clientes não distinguem “intent nunca persistido” (MR-002) de “plano indisponível”; corrupção de intent derruba report inteiro com 500 mesmo se decisions de pacotes forem válidas. |
| **Origin** | INTRODUCED_BY_BRANCH |
| **Recommended fix** | Propagar `plan_load_error` ou 404 parcial quando `PlanID` existe mas plano não; testes HTTP handler; considerar 422 para corrupção de intent vs 500 genérico. |

---

### MR-005 — Quota RATE_LIMITED: preservação OK

| Campo | Valor |
|-------|-------|
| **Severity** | LOW |
| **File:line** | `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/internal/profile/usage.go:237-258`; `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/internal/nexus/quota_monitor_service.go:174-199`; `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/internal/nexus/resource_discovery.go:103` |
| **Evidence** | Refresh falho preserva `lastKnown.Status==UsageRateLimited` com `Available=false`; monitor trata RATE_LIMITED como leitura confiável; `evaluateCandidate` rejeita `acc.RateLimited`. Teste `TestGetUsageSnapshotPreservesRateLimitedLastKnown` passa. |
| **Impact** | Sem regressão identificada para merge. Edge residual: preservação exige `len(lastKnown.Windows)>0`. |
| **Origin** | INTRODUCED_BY_BRANCH (refactor scoped usage) |
| **Recommended fix** | Nenhuma ação bloqueante; opcional: teste RATE_LIMITED sem windows. |

---

### MR-006 — Maestro lifecycle: não wired no runner

| Campo | Valor |
|-------|-------|
| **Severity** | HIGH |
| **File:line** | `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/internal/nexus/runner/runner.go:116-127`; `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/internal/nexus/maestro.go:712-758` |
| **Evidence** | Runner copia `MaestroSkills` → `SkillIDs` e persiste campo legacy; **nenhuma** chamada a `MaestroClient.GetAdvice` com `MaestroLifecycleTask/Verify/Recovery` no lifecycle. `MaestroGuidanceRef` em routing só preenche se `TaskRequirements.Guidance` já serializado (`mission_executor.go:259-271`). Compile mission usa snapshot congelado (`plan.go:196-210`), não consulta Maestro live. |
| **Impact** | Contrato Maestro lifecycle (TASK/VERIFY/RECOVERY) documentado mas inoperante em execução autônoma; routing report promete `maestro_guidance_ref` que raramente será populado em runtime real. |
| **Origin** | INTRODUCED_BY_BRANCH (projeção routing + campos legacy) |
| **Recommended fix** | Wire explícito: hooks em `StateCompiling`/`StateTesting`/`StateRemediating` chamando `GetAdvice` com lifecycle correto, ou marcar feature como deferred e remover campos do contrato público até implementar. |

---

### MR-007 — Contratos web ↔ nexus: regressões potenciais

| Campo | Valor |
|-------|-------|
| **Severity** | MEDIUM |
| **File:line** | `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/internal/control/web/handlers_planning.go:84-89`; `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/web/src/types.ts:1115-1139`; `/projetos/tools/IAPro-Nexus-Workspace-OS-Handoff-2026-08-29/ai-manager/web/src/nexus/api.ts:520-525` |
| **Evidence** | Handler POST plan retorna `store.WorkPlan` raw (201) sem validar presença de intent facts. TS `PackageRun` **não inclui** `routing_decision`, `provider`, `profile`, `desired_*` — backend persiste (`runner/types.go:201`). APIs `getRunRouting` / `getRunValidationEvidence` existem mas **nenhum consumidor** em `web/src/features/`. AutoPlan path (`GeneratePlanFromIntent`) também passa por `CreateWorkPlan` → MR-002. |
| **Impact** | UI que leia `MissionRun` diretamente perde routing inline; intent ausente passa despercebido; tipos TS desatualizados vs payload Go. |
| **Origin** | INTRODUCED_BY_BRANCH |
| **Recommended fix** | Alinhar `web/src/types.ts` com campos Go; validar intent facts na resposta POST plan (ou flag explícita); consumir `/routing` na superfície Flow ou documentar API-only. |

---

## Matriz por severidade

| Severity | IDs | Bloqueia merge? |
|----------|-----|-----------------|
| BLOCKER | — | — |
| HIGH | MR-002, MR-006 | Sim (MR-002); MR-006 se charter Maestro for exigido |
| MEDIUM | MR-001, MR-003, MR-004, MR-007 | Recomendado antes de merge |
| LOW | MR-005 | Não |

---

## Veredito merge

**NOT_READY_FOR_MAIN** — blocker lógico em **MR-002** (intent routing fail-open silencioso). PIN fail-closed em produção parece correto (MR-001), mas carece de prova integrada. Quota RATE_LIMITED (MR-005) está sólida. Maestro lifecycle (MR-006) permanece desconectado do runner apesar de campos/contratos expostos.

[REDACTED]