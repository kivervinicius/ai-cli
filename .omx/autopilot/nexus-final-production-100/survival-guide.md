# Survival Guide

Baseline is recorded in `DEV/finalization/SOURCE_BASELINE.md`. The tree was clean at `a1f3a57`; no commit/push is allowed automatically. Current highest-confidence defect is Workspace v1 plus ungated hydration/persistence. Next inspect tests and implement v2 migration while preserving existing automation. Platform runtime claims remain unverified until CI or native hosts are available.
